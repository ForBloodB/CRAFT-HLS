# /hls-review-contract

Review a contract before execution.

Run:

```bash
HLS-agent contract review <contract_dir>
```

Show:

- Phase-0 diagnosis type and evidence.
- Missing fields.
- HLS-Eval file summary.
- `contract_token_report.json` if present.

Do not run backend tools.
