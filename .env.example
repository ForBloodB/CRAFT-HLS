# 复制为 .env 或直接在 shell 中 export。
# API key 不会写入 SQLite；实验脚本只读取环境变量。
DEEPSEEK_API_KEY=填入你的DeepSeek API Key

# 如需使用本地 clone 的 HLS-Eval 数据集/工具，指向仓库根目录。
HLS_EVAL_ROOT=/home/distortionk/WorkSpace/VCS/HLS-Agent/external/hls-eval

